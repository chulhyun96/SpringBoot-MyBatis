<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Project1.repository.ProductRepository">
    <select id="findAll" resultType="ProductView">
        SELECT
        *
        FROM product
        <where>
            <if test="type == 'title'">
                name like '%${keyword}%'
            </if>

            <if test="type == 'description'">
                description like '%${keyword}%'
            </if>
        </where>
        ORDER BY id DESC
        limit #{offset},#{size}
    </select>

    <select id="count" resultType="int">
        SELECT
        count(id)
        FROM product
        <where>
            <if test="type == 'title'">
                name like '%${keyword}%'
            </if>

            <if test="type == 'description'">
                description like '%${keyword}%'
            </if>
        </where>
    </select>

    <select id="findById">
        SELECT
        *
        FROM product
        where id = #{id}
    </select>

    <insert id="reg" useGeneratedKeys="true" keyProperty="id">
        insert into product(name,selling_price, supplying_price, description ,img,category_id)
        values (#{name}, #{sellingPrice}, #{supplyingPrice}, #{description}, #{img},#{categoryId})
    </insert>

    <update id="updateProductById">
        UPDATE product
        SET name = #{name},selling_price = #{sellingPrice}, supplying_price = #{supplyingPrice}, description =
        #{description}, img = #{img} where id = #{id}
    </update>


    <delete id="deleteAll">
        DELETE FROM product WHERE id IN
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

</mapper>