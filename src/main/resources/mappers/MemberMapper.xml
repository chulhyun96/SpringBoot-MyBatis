<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Project1.web.repository.LoginRepository">

    <select id="findByLoginId">
        select * from member where user_id = #{loginId}
    </select>

</mapper>
